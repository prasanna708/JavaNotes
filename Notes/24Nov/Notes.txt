Collection Framework: 

Java provides an abstraction on top of data structures known as Collection Framework.

 A Collection is a list of items. 

 The Collection interface is extended by different classes of Collection Framewowkr:


Set interface: 

The implementation classes of Set interface are 

1. HashSet: 

The HashSet is used to store the elements with the following conditions.

* The order of the elements is unsual.

* The duplicate elements are ignored.
  

2. LinkedHashSet:


The LinkedHashSet is a sub class of Set Interface.

* LinkedHashSet gives the order.

* The duplicate elements are ignored.

PS: 

Create a Customer class with3 fields, add 3 customer objects to HashSet and display them on CONSOLE.


import java.util.HashSet;
import java.util.Set;

class Customer{
	 int custId; 
	 String name;
	 String address;
	public Customer(int custId, String name, String address) {
		super();
		this.custId = custId;
		this.name = name;
		this.address = address;
	}
	
	@Override
	public String toString() {
		System.out.println(this.custId);
		System.out.println(this.name);
		System.out.println(this.address);
		return "";
	}
	 
}
public class CustomerDemo {

	
	public static void main(String[] args) {
		
		 Set set = new HashSet();
		 Customer c1= new Customer(101, "Kiran", "Hyderabad");
		 Customer c2= new Customer(102, "Ramesh", "Banglore");
		 Customer c3= new Customer(103, "Naveen", "Chennai");
		 set.add(c1);
		 set.add(c2);
		 set.add(c3);
		 
		 System.out.println("c1:"+ c1);
		// System.out.println("Set:"+set);
		// System.out.println();
	}
}





